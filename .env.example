# Backend API base URL
# Development (Vite proxy rewrites /api → backend):
VITE_API_URL=/api

# If you need to call a remote backend directly during dev, use:
# VITE_API_URL=http://localhost:5000/api
